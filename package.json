{
  "name": "react-quiz",
  "version": "1.0.0",
  "private": true,
  "description": "Discover React-Quiz App: Educate and entertain with this interactive quiz, enhanced by the React useReducer hook. Choose your quiz level or question count easily. Answer in 30 seconds per question for a high score. Ready to play and learn?",
  "dependencies": {
    "@testing-library/jest-dom": "^5.17.0",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "json-server": "^0.17.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "server": "json-server --watch data/questions.json --port 8000",
    "dbServer": "json-server --watch data/db.json --port 9000"
  },
  "author" : "Rahul J",
  "license" : "MIT",
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
